### 智谱 GLM（OpenAI 兼容）— 请在下方 Environment 里配置 GLM_API_KEY，勿使用 PowerShell 的 $env: 写法
### JetBrains / VS Code REST Client：Authorization 使用 {{GLM_API_KEY}}，并在 http-client.env.json 或私有环境里填写密钥

@glm_base = https://open.bigmodel.cn/api/paas/v4

### 直连 chat/completions（确认密钥时用这一条）
POST {{glm_base}}/chat/completions
Content-Type: application/json
Authorization: Bearer {{GLM_API_KEY}}

{
  "model": "glm-5.1",
  "messages": [
    {
      "role": "user",
      "content": "只回复两个字：你好"
    }
  ],
  "temperature": 0.7
}

###
